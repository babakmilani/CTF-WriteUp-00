#include <iostream>
using namespace std;

//function prototypes
int option1();
int option2();
void option3();

int x,y, x_coord[11], y_coord[11];

int main() {

  int option = 0;

  do {
    cout << "Choose an option: " << endl;
    
    cout << "1) Store a new location: " << endl;
    cout << "2) Remove a location: " << endl;
    cout << "3) Show stored locations: " << endl;
    cin >> option;
    
    if(option == 1) {
      option1();
    }
    if(option == 2) {
      option2();
    }
    if(option == 3) {
      option3();
      exit(3);
    }
  } while (option !='3');

  return 0;
}


int option1() {
  
  cout << "Enter X coordinate: ";
  cin >> x;
  x_coord[x];
  cout << "Enter Y coordinate: ";
  cin >> y;
  y_coord[y];
  cout << "Location " << x << " , " << y << " has been stored. \n\n";
  return main();
}

int option2() {
  
}

void option3(int x_coord[], int y_coord[]) {
  for (int i ; i < x_coord[11]; i++) {
    cout << x_coord[i] << " " ;
  }
  cout << endl;
}

